# Quickstart: pboard configure (token argument)

## One-command setup

Copy your API token from ProductBoard and run:

```bash
pboard configure pb_your_token_here
```

Done. Verify it works:

```bash
pboard features list
```

## Interactive setup (still available)

```bash
pboard configure
# Enter your ProductBoard API token: <paste token>
```
